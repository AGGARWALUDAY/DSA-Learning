#include<iostream>
using namespace std;
bool checkissubsequence(vector<int>& nums,vector<int>& result,int n,int i,int a){

}
int f(vector<int>& nums,int n,int i){
    //Base Case 
    
    //Recursive Case
    push_back(nums[i]);
   if(checkissubsequence(nums,n,i,a)==true;){
        count++;
   }
}
{
vector<int> nums ={1,2,3,4};
int n=nums.size();
vector<int> result;
f(nums,,resultn,i);
}